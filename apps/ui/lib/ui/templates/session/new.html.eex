<h1>New Session</h1>

<%= form_for @conn, Routes.session_path(@conn, :create), [as: :player, autocomplete: "off"], fn f -> %>
  <%= text_input f, :name,
                    placeholder: "Name",
                    autofocus: true,
                    required: true %>
<% end %>
